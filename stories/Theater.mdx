import { Canvas, Meta } from '@storybook/blocks';

import  TheaterStories from './Theater.stories';

<Meta of={TheaterStories} />

# Theater

A layout with stage, curtain 

stage displays cards and an announcer can be put on the side displaying menu or form